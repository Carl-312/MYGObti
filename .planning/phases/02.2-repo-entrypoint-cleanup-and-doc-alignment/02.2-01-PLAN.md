---
phase: 02.2-repo-entrypoint-cleanup-and-doc-alignment
plan: 01
type: execute
wave: 1
depends_on:
  - "02.1-03"
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/NEXT-LONG-TASK-PROMPT.md
  - README.md
  - docs/ARCHITECTURE.md
  - .git-backups/**
  - mygobti@0.1.0
  - vite
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05
user_setup: []
must_haves:
  truths:
    - 不修改运行时 API、共享算法或业务玩法
    - 根目录主入口文档必须与当前 workspace 结构一致
    - 备份方式必须沿用现有 `.git-backups` 约定，不新增分支或提交
  artifacts:
    - .planning/**
    - README.md
    - docs/ARCHITECTURE.md
    - .git-backups/**
  key_links:
    - Phase 3 必须改为依赖 Phase 02.2，而不是继续直接依赖 Phase 02.1
    - `NEXT-LONG-TASK-PROMPT.md` 必须从已完成的 `02.1-03` 切回 `03-01`
---

<objective>
收口仓库主入口和执行真源，让后续成员与 agent 看到的入口说明和当前 workspace 现实一致。

Purpose: 减少旧单包路径、空遗留文件和过期 prompt 对后续执行的误导。
Output: 新的 `.git-backups` 快照、完整的 Phase `02.2` 文档骨架、对齐后的 README / 架构文档 / `.planning` 核心文件。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive the current repo state and insert Phase 02.2 into the roadmap/state flow</name>
  <files>.git-backups/**, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/02.2-repo-entrypoint-cleanup-and-doc-alignment/**</files>
  <read_first>.planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>按现有 `.git-backups` 约定创建一份新快照，然后手工补齐 `Phase 02.2` 的 roadmap、state 和 phase 文档骨架。要明确这次 phase 的职责是主入口收口，不是新功能开发；同时把下一步续接点重新指回 `Phase 3`。</action>
  <verify>git -C /home/carl/MYGObti status --short --branch</verify>
  <acceptance_criteria>
    - 新的 `.git-backups/<timestamp>/` 目录包含 bundle、diff、status 和 untracked 归档
    - `.planning/ROADMAP.md` 的顺序为 `2 -> 02.1 -> 02.2 -> 3`
    - `.planning/STATE.md` 已记录 `02.2` 完成，并把下一步指向 `Phase 3`
  </acceptance_criteria>
  <done>仓库快照和 Phase 02.2 执行骨架都已落盘。</done>
</task>

<task type="auto">
  <name>Task 2: Align entrypoint docs with the current workspace structure and remove empty root leftovers</name>
  <files>README.md, docs/ARCHITECTURE.md, .planning/PROJECT.md, .planning/NEXT-LONG-TASK-PROMPT.md, mygobti@0.1.0, vite</files>
  <read_first>README.md, docs/ARCHITECTURE.md, .planning/PROJECT.md, .planning/NEXT-LONG-TASK-PROMPT.md</read_first>
  <action>把仓库入口、运行方式、目录职责、历史材料与当前真源的说明统一到当前 workspace 结构；删除根目录无内容遗留文件。不要去重写历史 phase 文档，也不要扩展到 `questionedit/` 的内容整理。</action>
  <verify>rg -n \"02\\.1-03|^src/| vite\\.config\\.ts| index\\.html\" README.md docs/ARCHITECTURE.md .planning/*.md</verify>
  <acceptance_criteria>
    - 主入口文档不再把旧顶层 `src/**` 当作当前运行时入口
    - `NEXT-LONG-TASK-PROMPT.md` 不再推荐 `02.1-03`
    - 根目录不再保留无职责空文件
  </acceptance_criteria>
  <done>仓库主入口说明已经收口到实际代码结构。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.git-backups/<timestamp>/` 包含 `repo.bundle`、`status.txt`、`worktree.diff`、`index.diff`、`untracked.txt`、`untracked.tar.gz`
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] `rg -n "02\\.1-03|^src/| vite\\.config\\.ts| index\\.html" README.md docs/ARCHITECTURE.md .planning/*.md` 无过期主入口引用
- [ ] `git status --short --branch` 只体现本次预期改动
</verification>

<success_criteria>

- 备份方式与仓库既有约定一致
- `Phase 02.2` 已落到 roadmap 与 phase 文档
- README、架构文档和 `.planning` 核心文件已和当前 workspace 现实对齐
- 根目录不再保留无内容的旧入口占位文件

</success_criteria>

<output>
After completion, create `.planning/phases/02.2-repo-entrypoint-cleanup-and-doc-alignment/02.2-01-SUMMARY.md`
</output>

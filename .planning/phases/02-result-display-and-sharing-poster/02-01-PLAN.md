---
phase: 02-result-display-and-sharing-poster
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/App.tsx
  - src/pages/home/HomePage.tsx
  - src/app/styles.css
  - src/shared/types/quiz.ts
  - src/entities/character/model/characters.ts
autonomous: true
requirements:
  - RESULT-01
  - RESULT-02
  - CONTENT-03
  - UX-01
  - UX-02
user_setup: []
must_haves:
  truths:
    - 结果页从 MVP 文本块升级为更完整的移动端结果体验
    - 角色文案区域、三轴对比区域与再次测试入口都能在结果态中稳定出现
    - 隐藏祥子会被显式展示，但不会接管常规公开排名模型
  artifacts:
    - src/pages/home/HomePage.tsx
    - src/app/styles.css
    - src/shared/types/quiz.ts
  key_links:
    - 结果展示继续建立在 `evaluateQuizResult(...)` 的 ranking / tieBreak / hiddenMatch 输出上
    - 隐藏祥子展示只消费 `hiddenMatch`，不回写或篡改公开角色排名
---

<objective>
升级当前结果页的信息结构和视觉叙事，并把隐藏祥子的呈现策略落到真实 UI 上。

Purpose: 让用户从“看见一个结果块”升级为“走完一个完整结果体验”，同时为海报导出准备稳定的结果信息结构。
Output: 更强的结果 hero、角色文案区域、三轴对比区、隐藏祥子展示单元和再次测试入口。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-result-display-and-sharing-poster/02-CONTEXT.md
@.planning/phases/01-mvp-quiz-core/01-CONTEXT.md
@.planning/phases/01-mvp-quiz-core/01-03-PLAN.md
@start.md
@docs/ARCHITECTURE.md
@src/app/App.tsx
@src/pages/home/HomePage.tsx
@src/app/styles.css
@src/shared/types/quiz.ts
@src/entities/character/model/characters.ts
@src/features/quiz-engine/model/match.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure the result screen into a stronger mobile-first narrative</name>
  <files>src/pages/home/HomePage.tsx, src/app/App.tsx, src/app/styles.css</files>
  <read_first>start.md, src/pages/home/HomePage.tsx, src/features/quiz-engine/model/match.ts</read_first>
  <action>把当前 Phase 1 的结果块重组为真正的结果页结构：至少包含结果 hero、角色定位/文案区、三轴对比区，以及明显的再次测试入口。优先移动端首屏识别和滚动层级，不新建路由；如果 `HomePage.tsx` 继续变重，可以抽出 1-2 个轻量结果 section 组件，但不要为了形式把页面拆成过多目录。</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - 手机宽度下，用户进入结果页首屏就能读到角色名、称号和核心结论
    - 再次测试入口在结果态里始终清晰，不需要先滚回首页
    - 三轴对比继续基于已有 `axisBreakdown` 数据，不引入重图表库
  </acceptance_criteria>
  <done>结果页从 MVP 文本输出升级为具有明显层次和回看价值的完整体验。</done>
</task>

<task type="auto">
  <name>Task 2: Surface hidden Sakiko as a dedicated result signal without overwriting the public result</name>
  <files>src/pages/home/HomePage.tsx, src/app/styles.css, src/shared/types/quiz.ts, src/entities/character/model/characters.ts</files>
  <read_first>.planning/phases/02-result-display-and-sharing-poster/02-CONTEXT.md, src/shared/types/quiz.ts, src/entities/character/model/characters.ts</read_first>
  <action>把隐藏祥子从 Phase 1 的一行说明升级为独立视觉单元，例如隐藏命中卡、插层提示或 glitch 区块，明确说明她为何被触发、与公开主结果是什么关系。保持主结果仍由 `ranking[0]` 决定，隐藏祥子只作为彩蛋信号与补充解读存在；若确实需要补字段，只做最小类型与内容扩展，不重做匹配引擎。</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `hiddenMatch` 命中时，用户能明显看见隐藏祥子的独立呈现，而不再只是脚注
    - `hiddenMatch` 未命中时，不会留下空壳卡片或破坏普通结果布局
    - 代码中公开结果与隐藏彩蛋的职责边界可读，不会把两套结果来源写成一团
  </acceptance_criteria>
  <done>隐藏祥子的展示策略已经稳定，后续海报与分享可直接复用这个判定和文案关系。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] 手动验证一次普通公开结果页和一次隐藏祥子命中结果页
- [ ] 手机宽度下检查免责声明、再次测试入口和首屏信息层级
</verification>

<success_criteria>

- 结果体验明显强于 Phase 1 文本块，但仍保持单页、纯前端和轻量结构
- 隐藏祥子被明确呈现，同时不破坏 CONTENT-03 的独立触发原则
- 结果页信息结构已稳定到足以承接 Phase 2 的海报导出

</success_criteria>

<output>
After completion, create `.planning/phases/02-result-display-and-sharing-poster/02-01-SUMMARY.md`
</output>

---
phase: 02.3-template-driven-frontend-rebuild-and-gradual-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/main.tsx
  - apps/web/src/app/**
  - apps/web/src/shared/**
  - apps/web/src/pages/**
autonomous: true
requirements:
  - FRONTEND-01
  - FRONTEND-04
  - UX-01
  - UX-02
  - ARCH-03
user_setup:
  - 在独立终端常驻 `npm run dev:api`；若 `3001` 已被当前 API 进程占用，则沿用该监听进程
  - 在独立终端常驻 `npm run dev:web`，保持 `http://localhost:5173` 可持续访问
must_haves:
  truths:
    - 不允许一步删掉旧页面后再慢慢补新页面
    - 新模板基础设施必须落在 `apps/web`，不能把 `frontend-design/mygo-fronted` 当运行时入口
    - API 内容读取、结果计算和分享能力在本计划内只允许保持兼容，不允许顺手改语义
  artifacts:
    - apps/web/package.json
    - apps/web/src/app/**
    - apps/web/src/pages/**
  key_links:
    - 这一步要为后续聊天答题和结果页迁移先建立可并存的前端壳层
    - 浏览器应能在新壳层就位后继续回退到旧答题主链路
---

<objective>
把模板化前端重构的基础设施先落到 `apps/web`，建立可渐进迁移的新壳层和旧实现边界。

Purpose: 让后续聊天 UI 和结果页替换不再直接冲撞当前一体化 `HomePage`。
Output: 可运行的新前端 shell、基础动效依赖、旧页面的迁移边界，以及贯穿后续所有计划的常驻 dev server + 浏览器验收节奏。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-CONTEXT.md
@apps/web/package.json
@apps/web/src/app/App.tsx
@apps/web/src/pages/home/HomePage.tsx
@frontend-design/mygo-fronted/package.json
@frontend-design/mygo-fronted/src/components/layout/PageShell.tsx
@frontend-design/mygo-fronted/src/components/layout/PageTransition.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce the template runtime foundation into apps/web without cutting over the whole UI</name>
  <files>apps/web/package.json, apps/web/src/main.tsx, apps/web/src/app/**</files>
  <read_first>frontend-design/mygo-fronted/package.json, frontend-design/mygo-fronted/src/components/layout/PageShell.tsx, frontend-design/mygo-fronted/src/components/layout/PageTransition.tsx</read_first>
  <action>把模板所需的前端基础设施先接入 `apps/web`，包括动效依赖、页面壳层、背景覆层、必要的全局 tokens 和转场基建，但先不要把 intro / quiz / result 一口气全切过去。目标是先让当前应用能在新壳层或新 route 边界下稳定运行，为后续逐段替换腾出空间。</action>
  <verify>npm run typecheck:web</verify>
  <acceptance_criteria>
    - `apps/web` 可以在新壳层下正常启动
    - 旧页面逻辑仍可通过迁移边界继续访问，不会因为新壳层接入就断链
    - 模板基础设施接入后不会破坏 `/api` 读取和当前加载态
  </acceptance_criteria>
  <done>新模板的运行时基础已经进入生产应用，后续不必再在旧大组件里硬塞新交互。</done>
</task>

<task type="auto">
  <name>Task 2: Carve out a migration-safe boundary from the current HomePage monolith</name>
  <files>apps/web/src/pages/**, apps/web/src/app/App.tsx</files>
  <read_first>apps/web/src/pages/home/HomePage.tsx, apps/web/src/app/App.tsx</read_first>
  <action>把当前 `HomePage` 按 intro / answering / completed 的职责拆成可替换边界，至少要让后续计划能逐段迁移，而不是继续在单文件里叠更多条件分支。可以保留 legacy screen 作为过渡，但要把新旧入口关系写清楚，让后续 plan 能稳定替换其中一段。</action>
  <verify>npm run build:web</verify>
  <acceptance_criteria>
    - `HomePage` 不再是唯一不可拆的前端入口
    - 后续计划可以只替换答题链路或结果页，不需要同时重写 intro / result / share
    - 浏览器内可以明确区分新壳层和旧链路的衔接点
  </acceptance_criteria>
  <done>大组件已经被切出迁移边界，后续每个计划都能独立落点并单独验收。</done>
</task>

<task type="auto">
  <name>Task 3: Lock in the persistent dev-server QA loop before any deeper UI migration</name>
  <files>.planning/NEXT-LONG-TASK-PROMPT.md, apps/web/src/app/**</files>
  <read_first>.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-CONTEXT.md</read_first>
  <action>把“API 与 Web 开发服务器常驻、浏览器逐 plan 验收”的节奏落成可执行约束。必要时在应用里补轻量 debug affordance 或迁移提示，让后续 plan 可以明确知道应该在浏览器看哪里，而不是只跑类型检查。</action>
  <verify>curl -I http://localhost:5173</verify>
  <acceptance_criteria>
    - `npm run dev:api` 与 `npm run dev:web` 的常驻策略在本计划结束时已明确可用
    - 后续计划拥有可复用的浏览器验收入口或明确检查点
    - 新壳层接入后，开发者不需要频繁重启服务才能继续迁移
  </acceptance_criteria>
  <done>执行节奏先被锁住，后面所有 UI 迁移都将围绕常驻服务和浏览器验收推进。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck:web`
- [ ] `npm run build:web`
- [ ] `curl http://127.0.0.1:3001/api/quiz/meta`
- [ ] 浏览器打开 `http://localhost:5173`，确认新壳层就位后仍能继续走到旧答题链路
- [ ] 不关闭常驻的 web/api dev server 进程
</verification>

<success_criteria>

- 新模板基础设施已经进入 `apps/web`
- 旧一体化页面被切出迁移边界
- 常驻 dev server 与浏览器验收节奏在计划 1 就被锁定

</success_criteria>

<output>
After completion, create `.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-01-SUMMARY.md`
</output>

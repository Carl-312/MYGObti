---
phase: 02.3-template-driven-frontend-rebuild-and-gradual-migration
plan: 03
type: execute
wave: 3
depends_on:
  - "02.3-02"
files_modified:
  - apps/web/src/app/App.tsx
  - apps/web/src/pages/**
  - apps/web/src/features/**
  - apps/web/src/shared/**
autonomous: true
requirements:
  - FRONTEND-01
  - FRONTEND-02
  - FRONTEND-04
  - UX-01
  - UX-02
  - ARCH-03
  - ARCH-04
user_setup:
  - 保持 `npm run dev:api` 常驻
  - 保持 `npm run dev:web` 常驻，并使用浏览器持续验收答题主链路
must_haves:
  truths:
    - 做题必须真正像聊天，而不是旧卡片外面包一层对话皮
    - 现有评分状态机、API 内容读取和答案记录语义必须继续复用
    - 用户仍要能修改或回看上一题答案，但交互形态不能把聊天感完全打回传统表单
  artifacts:
    - apps/web/src/app/App.tsx
    - apps/web/src/pages/**
    - apps/web/src/features/**
  key_links:
    - 新答题流必须直接消费 02.3-02 的聊天原子和素材 resolver
    - 计划完成后，结果页输入数据必须仍与当前 `MatchComputation` 结构兼容
---

<objective>
把当前卡片式答题主链路迁到真正的 LINE 式聊天界面，同时保留现有状态机和结果计算。

Purpose: 让“做题像群聊”从预览文案变成真实交互，而不是继续停留在首页的 preview 气泡。
Output: 可连续作答的聊天式 quiz flow、即时发送动效、可回看/修改答案的过渡交互，以及不变的 `MatchComputation` 输入输出。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-CONTEXT.md
@apps/web/src/app/App.tsx
@apps/web/src/pages/home/HomePage.tsx
@frontend-design/mygo-fronted/src/components/chat/DialogueRow.tsx
@frontend-design/mygo-fronted/src/components/chat/DialogueList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt the current quiz question state into a chat-scene data model</name>
  <files>apps/web/src/features/**, apps/web/src/pages/**</files>
  <read_first>apps/web/src/app/App.tsx, apps/web/src/pages/home/HomePage.tsx</read_first>
  <action>基于现有 `currentQuestionIndex`、`answers`、`submitMessage` 和 question data，建立新的 chat-scene adapter。这个 adapter 要把“系统提问、场景提示、用户已选答案、待选项”翻成消息流，让 UI 不再直接依赖旧 `question-frame` 卡片结构。</action>
  <verify>npm run typecheck:web</verify>
  <acceptance_criteria>
    - 答题页面不再直接围绕旧 `question-frame` 组织 UI
    - 聊天流能正确表达当前题、已答题和下一步可选项
    - adapter 不改动 `packages/quiz-core` 或 API payload 结构
  </acceptance_criteria>
  <done>聊天 UI 已经拥有自己的场景数据层，而不是继续向旧卡片实现借身体。</done>
</task>

<task type="auto">
  <name>Task 2: Implement the LINE-like answering experience with send animation and auto-scroll</name>
  <files>apps/web/src/pages/**, apps/web/src/features/**</files>
  <read_first>frontend-design/mygo-fronted/src/components/chat/DialogueRow.tsx, frontend-design/mygo-fronted/src/components/chat/DialogueList.tsx</read_first>
  <action>把答题主链路真正迁成聊天界面：系统题目和 scene hint 作为左侧消息，用户选项以右侧 choice bubble 呈现；点击后触发发送动效、其他未选项淡出、消息列表自动滚动，然后进入下一题。必要时补 typing indicator 或 stage transition，但不要重新造一套脱离模板语义的动画。</action>
  <verify>npm run build:web</verify>
  <acceptance_criteria>
    - 连续答题时的视觉体验像消息往下掉，而不是分页卡片切换
    - 选择一个选项后能看到发送反馈和下一题衔接
    - 在浏览器中连续作答多题时，滚动、动画和阅读节奏都稳定
  </acceptance_criteria>
  <done>“像 LINE 聊天一样做题”已经从需求变成真实可玩的主链路。</done>
</task>

<task type="auto">
  <name>Task 3: Preserve answer revision without collapsing back into the old card UI</name>
  <files>apps/web/src/pages/**, apps/web/src/features/**</files>
  <read_first>apps/web/src/pages/home/HomePage.tsx</read_first>
  <action>为聊天式答题补一个不破坏气氛的回退/修改答案机制。可以是底部 action、最近消息处的“改答案”、或局部跳转面板，但用户仍然要能改上一题或定位未答题，而不是因为换成聊天 UI 就失去原有修正能力。</action>
  <verify>curl -I http://localhost:5173</verify>
  <acceptance_criteria>
    - 用户能够回看或修改答案
    - 这一能力不会让界面退回旧的整页卡片交互
    - 未答完直接提交时，仍能正确引导回未作答位置
  </acceptance_criteria>
  <done>聊天体验和可修正性同时保住，旧答题链路的关键可用性没有丢。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck:web`
- [ ] `npm run build:web`
- [ ] 浏览器连续作答至少 5 题，确认聊天流、发送动效和自动滚动稳定
- [ ] 浏览器验证“返回 / 修改答案 / 未答题引导”仍可用
- [ ] 不关闭常驻的 web/api dev server 进程
</verification>

<success_criteria>

- 答题主链路已迁成聊天式消息流
- 原有评分状态机和 API 内容读取保持不变
- 用户仍能在聊天体验下安全修改答案和完成整套题

</success_criteria>

<output>
After completion, create `.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-03-SUMMARY.md`
</output>

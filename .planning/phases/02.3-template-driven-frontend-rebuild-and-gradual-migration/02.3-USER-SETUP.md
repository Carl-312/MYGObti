# Phase 02.3: User Setup Required

**Generated:** 2026-04-22
**Phase:** template-driven-frontend-rebuild-and-gradual-migration
**Status:** Incomplete

完成下面这些本地运行项后，Phase `02.3` 的浏览器验收节奏才算稳定。

## Environment Variables

None - 当前计划不需要新增 secret 或第三方服务账号。

## Local Development

- [ ] **保持 Web dev server 常驻**
  - Command: `npm run dev:web`
  - Expected URL: `http://127.0.0.1:5173`
  - Notes: 如果已有可访问的 `5173` 进程，就直接复用，不要每个 plan 都重启。

- [ ] **保持 API dev server 常驻**
  - Command: `npm run dev:api`
  - Expected URL: `http://127.0.0.1:3001`
  - Notes: 本次执行里默认 `3001` 被一个非响应式本地监听占住，导致 `tsx watch` 无法直接 bind；继续执行下个 plan 前，请先清掉这个脏端口占用。

- [ ] **端口占用未清理前的临时 smoke check**
  - Command: `PORT=3101 npm run dev:api`
  - Expected URL: `http://127.0.0.1:3101/api/quiz/meta`
  - Notes: 这只是为了验证 API 代码路径没有坏，不替代正式的 `3001` QA 标准。

## Verification

完成本地设置后，用下面这些命令复核：

```bash
curl --noproxy '*' -sf http://127.0.0.1:5173
curl --noproxy '*' -sf http://127.0.0.1:3001/api/quiz/meta
```

Expected results:
- `5173` 返回 Vite 首页 HTML
- `3001/api/quiz/meta` 返回 canonical quiz meta JSON
- 浏览器打开 `http://127.0.0.1:5173` 时，能看到新的 template shell 与 legacy migration boundary

---

**Once all items complete:** Mark status as "Complete" at top of file.

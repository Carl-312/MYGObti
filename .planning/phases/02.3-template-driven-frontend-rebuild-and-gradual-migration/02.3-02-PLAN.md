---
phase: 02.3-template-driven-frontend-rebuild-and-gradual-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "02.3-01"
files_modified:
  - apps/web/src/features/**
  - apps/web/src/shared/**
  - apps/web/src/entities/character/**
  - apps/web/public/live2d/**
  - apps/web/public/round-icons/**
autonomous: true
requirements:
  - FRONTEND-01
  - FRONTEND-02
  - FRONTEND-03
  - FRONTEND-04
  - ARCH-04
user_setup:
  - 保持 `npm run dev:api` 常驻
  - 保持 `npm run dev:web` 常驻，并用浏览器持续打开 `http://localhost:5173`
must_haves:
  truths:
    - 聊天气泡和角色资源能力必须先抽成可复用原子，再去迁答题和结果页
    - 8 角色头像与 Live2D 资源必须通过 manifest 解析，不允许到处手写路径
    - 模板里的 `DialogueRow` 动效语义必须优先复用，而不是重新做一套近似效果
  artifacts:
    - apps/web/src/features/**
    - apps/web/src/shared/**
    - apps/web/public/live2d/**
    - apps/web/public/round-icons/**
  key_links:
    - 02.3-03 会直接消费这里抽出的聊天消息原子和角色素材 resolver
    - 02.3-04 会直接消费这里定义的圆形头像 / Live2D 槽位 contract
---

<objective>
先把聊天消息原子和角色资源 contract 抽出来，让后续答题与结果页重构有稳定可复用底座。

Purpose: 避免答题页和结果页各自复制一套聊天 UI 与角色素材逻辑。
Output: 可复用的 `DialogueRow` / `DialogueList` 迁移版、角色头像与 Live2D resolver，以及可单独浏览器验收的素材槽位组件。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-CONTEXT.md
@frontend-design/mygo-fronted/src/components/chat/DialogueRow.tsx
@frontend-design/mygo-fronted/src/components/chat/DialogueList.tsx
@apps/web/src/features/band-story/ui/DialogueRow.tsx
@apps/web/public/live2d/manifest.json
@apps/web/public/round-icons/manifest.json
@apps/web/src/entities/character/model/characters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the template chat atoms into apps/web as production UI primitives</name>
  <files>apps/web/src/features/**, apps/web/src/shared/**</files>
  <read_first>frontend-design/mygo-fronted/src/components/chat/DialogueRow.tsx, frontend-design/mygo-fronted/src/components/chat/DialogueList.tsx, apps/web/src/features/band-story/ui/DialogueRow.tsx</read_first>
  <action>把模板里的 `DialogueAvatar`、`DialogueBubble`、`DialogueRow`、`DialogueList` 和相关 message type 迁入 `apps/web` 的生产目录，保留它们的 row entrance、send animation、auto-scroll 和 choice fade 语义。可以参考当前 `band-story` 的命名和目录习惯，但不要退化成无动效的简化版。</action>
  <verify>npm run typecheck:web</verify>
  <acceptance_criteria>
    - `apps/web` 内已有可复用的生产级聊天 UI 原子
    - 动效 contract 与模板实现语义保持一致
    - 这些原子不依赖 demo 数据或模板目录才能运行
  </acceptance_criteria>
  <done>聊天消息系统正式落进运行时应用，答题流可以开始真正迁成对话界面。</done>
</task>

<task type="auto">
  <name>Task 2: Build a manifest-driven character asset resolver for round avatars and Live2D slots</name>
  <files>apps/web/src/entities/character/**, apps/web/src/shared/**</files>
  <read_first>apps/web/public/live2d/manifest.json, apps/web/public/round-icons/manifest.json, apps/web/src/entities/character/model/characters.ts</read_first>
  <action>为 8 个角色建立统一的资源解析层：输入角色 id，输出圆形头像、Live2D 静态图、缺省占位与展示元信息。目标是让结果页、聊天头像和未来其他角色卡片都从一个 resolver 取资源，而不是在组件内拼字符串。</action>
  <verify>npm run build:web</verify>
  <acceptance_criteria>
    - 所有公开角色和隐藏祥子都能通过统一 resolver 拿到头像和 Live2D 信息
    - 缺图时有明确 placeholder 行为，而不是组件直接崩溃
    - 角色资源 contract 与角色 profile id 保持一一对应
  </acceptance_criteria>
  <done>角色素材终于从“散落在 public 目录”变成前端可消费的正式 contract。</done>
</task>

<task type="auto">
  <name>Task 3: Expose a browser-verifiable preview surface for chat atoms and asset slots</name>
  <files>apps/web/src/pages/**, apps/web/src/features/**</files>
  <read_first>.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-CONTEXT.md</read_first>
  <action>给这些新原子补一个轻量浏览器预览入口，可以是 dev-only route、迁移中的 preview panel，或明确嵌进新壳层的测试场景。重点不是做 storybook，而是让后续计划能在真实浏览器里快速验证消息动效、圆形头像和 Live2D 槽位已经可用。</action>
  <verify>curl -I http://localhost:5173</verify>
  <acceptance_criteria>
    - 浏览器中能看到聊天行、选择态、圆形头像和 Live2D 槽位的实际表现
    - 后续计划不需要先接完整答题流程才能验证素材和消息原子
    - 常驻 dev server 环境下可以持续刷新和观察这些原子
  </acceptance_criteria>
  <done>所有关键前端原子都拥有真实浏览器验收面，后续迁移不再盲飞。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck:web`
- [ ] `npm run build:web`
- [ ] 浏览器中验证 `DialogueRow` 进场动画、选择发送动画和 auto-scroll
- [ ] 浏览器中验证 8 个角色的圆形头像与 Live2D 槽位都能被 resolver 命中或正确回退
- [ ] 不关闭常驻的 web/api dev server 进程
</verification>

<success_criteria>

- 聊天 UI 原子已从模板迁入生产代码
- 8 角色素材路径已收口为统一 resolver
- 后续答题流和结果页都能直接复用这些基座

</success_criteria>

<output>
After completion, create `.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-02-SUMMARY.md`
</output>

---
phase: 02.3-template-driven-frontend-rebuild-and-gradual-migration
plan: 05
type: execute
wave: 5
depends_on:
  - "02.3-04"
files_modified:
  - apps/web/src/pages/**
  - apps/web/src/app/styles.css
  - apps/web/src/test/**
  - README.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements:
  - FRONTEND-01
  - FRONTEND-04
  - RESULT-01
  - RESULT-02
  - RESULT-03
user_setup:
  - 保持 `npm run dev:api` 常驻直到最终浏览器回归完成
  - 保持 `npm run dev:web` 常驻直到旧实现清理完成并回归通过
must_haves:
  truths:
    - 旧实现只能在新 intro / quiz / result 链路全部浏览器验收通过后再删除
    - 清理时要优先删死代码和旧样式，不要误删分享或 API 读取能力
    - phase 结束时必须留下可持续接手的文档和测试入口
  artifacts:
    - apps/web/src/pages/**
    - apps/web/src/app/styles.css
    - apps/web/src/test/**
    - README.md
    - docs/ARCHITECTURE.md
  key_links:
    - 本计划会为后续 Phase 3 的内容深化提供稳定 UI 基线
    - 旧 `HomePage` 和旧样式只能在新流完全可回归后清理
---

<objective>
在浏览器验收通过后清理旧实现、补齐回归与文档，让新的模板化前端成为唯一稳定入口。

Purpose: 防止这次重构最后停留在“新旧两套并存但没人敢删”的中间态。
Output: 清理后的新前端真源、针对新链路的回归校验、更新过的项目文档与稳定的后续接手入口。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-CONTEXT.md
@apps/web/src/pages/home/HomePage.tsx
@apps/web/src/app/styles.css
@README.md
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy page branches and obsolete styles only after the new flow is browser-accepted</name>
  <files>apps/web/src/pages/**, apps/web/src/app/styles.css</files>
  <read_first>apps/web/src/pages/home/HomePage.tsx, apps/web/src/app/styles.css</read_first>
  <action>在确认新 intro / chat quiz / result 全链路都已通过浏览器验收后，删除旧 `HomePage` 分支、旧卡片式答题结构和明显失效的旧样式。清理时要保留仍被新页面复用的 share 能力、通用 helper 和必要 fallback，不要做误删式大扫除。</action>
  <verify>npm run build:web</verify>
  <acceptance_criteria>
    - 旧主链路和死样式已从生产代码中清理
    - 新前端成为唯一主入口，不再同时维护两套核心页面
    - 清理后结果页、分享导出和 API 读取仍正常
  </acceptance_criteria>
  <done>迁移正式收口，新旧并存的过渡态被清理干净。</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage and execution notes for the rebuilt frontend</name>
  <files>apps/web/src/test/**, README.md, docs/ARCHITECTURE.md</files>
  <read_first>README.md, docs/ARCHITECTURE.md</read_first>
  <action>针对新前端链路补最低限度的回归测试和执行说明，至少覆盖加载内容、进入聊天答题、生成结果和导出分享海报的关键路径。文档中要写清楚常驻 dev server、浏览器验收与角色素材 manifest 的位置，保证后续 agent 接手时不会回头读旧说明。</action>
  <verify>npm run test --workspace apps/web</verify>
  <acceptance_criteria>
    - 新前端关键链路拥有最基本的测试或可复用的验收步骤
    - README / ARCHITECTURE 不再把旧 `HomePage` 和旧样式当现行入口
    - 后续 Phase 3 可以基于新前端直接做内容深化，而不是再次整理 UI 边界
  </acceptance_criteria>
  <done>这次重构不仅能跑，而且留下了下一位执行者真正能继续接手的入口。</done>
</task>

<task type="auto">
  <name>Task 3: Run the final in-browser regression with persistent dev servers before closing the phase</name>
  <files>apps/web/src/pages/**, apps/web/src/features/**</files>
  <read_first>.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-CONTEXT.md</read_first>
  <action>在不重启现有 dev server 的前提下，完成最终浏览器回归：加载态、首页入口、聊天式答题、未答题回退、结果页 8 角色素材、分享导出和重测入口都要走一遍。只有这一步完成后，Phase `02.3` 才能视为真正结束。</action>
  <verify>curl -I http://localhost:5173</verify>
  <acceptance_criteria>
    - 整条前端主链路已在真实浏览器下回归通过
    - 常驻 dev server 模式证明这套迁移节奏可以持续工作，而不是靠反复重启碰运气
    - 可以明确宣布旧实现不再是 fallback 依赖
  </acceptance_criteria>
  <done>Phase `02.3` 以真实浏览器回归而不是代码层自我感觉良好收尾。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck:web`
- [ ] `npm run build:web`
- [ ] `npm run test --workspace apps/web`
- [ ] 浏览器全链路回归：加载 -> intro -> 聊天答题 -> 结果页 -> 分享导出 -> 重测
- [ ] 浏览器复核 8 角色素材与头像框位
- [ ] 直到全部通过前，不关闭常驻的 web/api dev server 进程
</verification>

<success_criteria>

- 新前端成为唯一稳定入口
- 旧主链路和旧样式已被安全清理
- 文档、测试和浏览器回归都已跟上这次重构

</success_criteria>

<output>
After completion, create `.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-05-SUMMARY.md`
</output>

---
phase: 02.3-template-driven-frontend-rebuild-and-gradual-migration
plan: 04
type: execute
wave: 4
depends_on:
  - "02.3-03"
files_modified:
  - apps/web/src/pages/**
  - apps/web/src/features/share/**
  - apps/web/src/features/**
  - apps/web/src/shared/**
  - apps/web/src/entities/character/**
autonomous: true
requirements:
  - FRONTEND-01
  - FRONTEND-03
  - FRONTEND-04
  - RESULT-01
  - RESULT-02
  - RESULT-03
  - UX-01
user_setup:
  - 保持 `npm run dev:api` 常驻
  - 保持 `npm run dev:web` 常驻，并在浏览器中反复切换结果态做验收
must_haves:
  truths:
    - 结果页必须接上 8 角色圆形头像和 Live2D 静态图槽位
    - 现有分享海报导出和分享降级能力必须继续可用
    - 公开榜首、隐藏信号、三轴对比和候选榜单语义不能因为样式重构而消失
  artifacts:
    - apps/web/src/pages/**
    - apps/web/src/features/share/**
    - apps/web/src/entities/character/**
  key_links:
    - 02.3-02 的资源 resolver 会直接供结果页 hero、头像框和关系卡片使用
    - 结果页必须仍以 `MatchComputation` 为真源，不允许复制第二套结果模型
---

<objective>
把结果页改造成模板化角色报告，并把 8 角色素材槽位与分享工作台真正接到新页面上。

Purpose: 让结果页不再只是旧 story-card 的拼贴，而是与聊天答题同语言的角色终章。
Output: 新的结果 hero、圆形头像与 Live2D 区块、保持可导出的分享海报，以及针对 8 角色的浏览器验收面。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-CONTEXT.md
@apps/web/src/pages/home/HomePage.tsx
@apps/web/src/features/share/ui/ResultPoster.tsx
@apps/web/public/live2d/manifest.json
@apps/web/public/round-icons/manifest.json
@apps/web/src/entities/character/model/characters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the result page into modular template-era sections</name>
  <files>apps/web/src/pages/**, apps/web/src/features/**, apps/web/src/shared/**</files>
  <read_first>apps/web/src/pages/home/HomePage.tsx, apps/web/src/features/share/ui/ResultPoster.tsx</read_first>
  <action>把当前 `completed` 段的大型结果页拆成新的模块化 section：结果 hero、角色关系、结论补充、三轴对比、榜单、share workspace。目标是让结果页结构和聊天答题一样，进入新模板语言，而不是继续挂在旧 `HomePage` 的大条件分支下面。</action>
  <verify>npm run typecheck:web</verify>
  <acceptance_criteria>
    - 结果页已脱离旧 `HomePage` 的 completed 大段 JSX
    - 公开主结果、隐藏信号、三轴对比、榜单和重测入口都仍存在
    - 模块结构可以独立维护，不再需要在一个文件里同时改所有结果 UI
  </acceptance_criteria>
  <done>结果页终于拥有与新前端相匹配的模块边界。</done>
</task>

<task type="auto">
  <name>Task 2: Wire circular avatar frames and Live2D image slots for all 8 characters</name>
  <files>apps/web/src/entities/character/**, apps/web/src/pages/**, apps/web/src/shared/**</files>
  <read_first>apps/web/public/live2d/manifest.json, apps/web/public/round-icons/manifest.json, apps/web/src/entities/character/model/characters.ts</read_first>
  <action>把结果页 hero、关系卡、聊天复盘或其他关键角色位都接到圆形头像和 Live2D 静态图槽位上。设计上要明确预留视觉位置，但实现上用 manifest resolver 接通真实资源，确保 8 个角色都能命中各自素材，缺失时也有明确 fallback。</action>
  <verify>npm run build:web</verify>
  <acceptance_criteria>
    - 8 个角色都能在结果页命中头像与 Live2D 资源
    - 圆形头像框位不再依赖临时文本 badge 充数
    - Live2D 静态图区块具有稳定布局，不会因为图片尺寸差异把结果页撑坏
  </acceptance_criteria>
  <done>用户终于能在结果页直接看到完整角色素材，而不是文字说明和临时占位。</done>
</task>

<task type="auto">
  <name>Task 3: Preserve poster export and add a browser-friendly way to verify all character result variants</name>
  <files>apps/web/src/features/share/**, apps/web/src/pages/**</files>
  <read_first>apps/web/src/features/share/ui/ResultPoster.tsx</read_first>
  <action>保持现有 `ResultPoster` 导出、下载和移动端分享降级链路可用；同时补一个适合开发验收的结果预览方式，让开发阶段可以快速检查 8 个角色的结果素材和布局，不需要每次完整答题才能看到对应结果。</action>
  <verify>curl -I http://localhost:5173</verify>
  <acceptance_criteria>
    - 分享海报仍能正确导出
    - 至少有一种开发态方式可以快速切换不同角色结果做浏览器验收
    - 结果页素材接入不会把分享区域一起搞坏
  </acceptance_criteria>
  <done>结果页完成真正可验收的模板化落地，而不是“主结果好了，剩下 7 个角色以后再看”。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck:web`
- [ ] `npm run build:web`
- [ ] 浏览器中验证结果页 hero、关系线、榜单、隐藏信号与分享操作均可用
- [ ] 浏览器中逐个检查 8 个角色的圆形头像与 Live2D 图槽位
- [ ] 手动验证一次分享海报导出和一次分享降级路径
- [ ] 不关闭常驻的 web/api dev server 进程
</verification>

<success_criteria>

- 结果页已进入新模板语言并完成模块化
- 8 角色素材槽位已全部接通
- 分享海报与结果可视化在重构后仍然稳定

</success_criteria>

<output>
After completion, create `.planning/phases/02.3-template-driven-frontend-rebuild-and-gradual-migration/02.3-04-SUMMARY.md`
</output>

---
phase: 02.1-frontend-backend-separation-and-content-service-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - "02.1-01"
files_modified:
  - apps/api/src/server.ts
  - apps/api/src/routes/health.ts
  - apps/api/src/routes/quiz.ts
  - apps/api/src/services/canonicalContent.ts
  - apps/api/src/plugins/cors.ts
  - questionedit/questionnewV2.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-05
user_setup: []
must_haves:
  truths:
    - 运行时 canonical 题库不能再由浏览器直接解析 Markdown
    - 后端首阶段只需要只读内容 API，不需要数据库和用户态
    - API 输出必须与当前 V2.1D 规则语义保持一致
  artifacts:
    - apps/api/src/server.ts
    - apps/api/src/routes/quiz.ts
    - apps/api/src/services/canonicalContent.ts
  key_links:
    - API 的内容序列化必须复用现有 canonical 解析规则
    - 对外 contract 必须能被 web 直接消费，不再依赖 `?raw`
---

<objective>
建立轻量 Fastify 内容服务，把 canonical 内容真源迁出浏览器。

Purpose: 让运行时内容真源回到后端边界，从而为后续题库深化和校准提供稳定 API。
Output: 可启动的 Fastify API、健康检查、quiz meta/content 只读接口，以及与当前 V2.1D 一致的 canonical 输出。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-frontend-backend-separation-and-content-service-refactor/02.1-CONTEXT.md
@packages/quiz-core/src/index.ts
@src/entities/quiz/model/canonicalQuiz.ts
@questionedit/questionnewV2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move canonical parsing and normalization into the API layer</name>
  <files>apps/api/src/services/canonicalContent.ts, questionedit/questionnewV2.md</files>
  <read_first>src/entities/quiz/model/canonicalQuiz.ts, questionedit/questionnewV2.md</read_first>
  <action>把当前 `canonicalQuiz.ts` 里依赖 Markdown raw import 的解析逻辑迁到 API 服务层，并整理成明确的 canonical content service。服务可以在启动时解析并缓存，或按需懒加载，但最终输出必须保持与当前浏览器侧 V2.1D 结构语义一致。</action>
  <verify>npm run build --workspace apps/api</verify>
  <acceptance_criteria>
    - canonical 解析不再依赖 Vite `?raw`
    - 输出包含 quiz meta、questions、characters 等前端所需运行时数据
    - 同一份 `questionnewV2.md` 解析结果与现有前端行为保持一致
  </acceptance_criteria>
  <done>后端成为 canonical 内容运行时真源，浏览器不再解析 Markdown。</done>
</task>

<task type="auto">
  <name>Task 2: Expose read-only Fastify endpoints for health and quiz content</name>
  <files>apps/api/src/server.ts, apps/api/src/routes/health.ts, apps/api/src/routes/quiz.ts, apps/api/src/plugins/cors.ts</files>
  <read_first>.planning/phases/02.1-frontend-backend-separation-and-content-service-refactor/02.1-CONTEXT.md, apps/api/src/services/canonicalContent.ts</read_first>
  <action>使用 Fastify 搭出轻量只读 API。至少提供 `/api/health`、`/api/quiz/meta`、`/api/quiz/content`，并按当前官方文档习惯使用插件注册和 schema-based route 定义。开发态允许本地联调，非代理场景下仅配置必要的 CORS，避免默认全开。</action>
  <verify>npm run typecheck --workspace apps/api</verify>
  <acceptance_criteria>
    - API 可独立启动并返回结构稳定的 JSON
    - 健康检查端点可用于部署与联调确认
    - 后端没有引入数据库、登录或写接口
  </acceptance_criteria>
  <done>内容服务边界稳定，前端迁移所需的数据接口已经齐备。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck --workspace apps/api`
- [ ] `npm run build --workspace apps/api`
- [ ] 手动调用 `/api/health`
- [ ] 手动调用 `/api/quiz/meta`
- [ ] 手动调用 `/api/quiz/content`
</verification>

<success_criteria>

- Fastify API 稳定输出运行时内容真源
- 浏览器不再承担 Markdown canonical 解析职责
- Phase 3 的内容深化可以围绕统一 API contract 继续推进

</success_criteria>

<output>
After completion, create `.planning/phases/02.1-frontend-backend-separation-and-content-service-refactor/02.1-02-SUMMARY.md`
</output>

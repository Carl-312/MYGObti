---
phase: 02.1-frontend-backend-separation-and-content-service-refactor
plan: 03
type: execute
wave: 3
depends_on:
  - "02.1-01"
  - "02.1-02"
files_modified:
  - apps/web/src/app/App.tsx
  - apps/web/src/pages/home/HomePage.tsx
  - apps/web/src/features/**/*
  - apps/web/vite.config.ts
  - apps/web/src/vite-env.d.ts
  - README.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements:
  - ARCH-03
  - ARCH-04
  - UX-02
  - UX-03
user_setup: []
must_haves:
  truths:
    - 前端必须改为通过 API 读取运行时内容，而不是直接导入 Markdown
    - 开发体验要尽量维持简单，本地启动时应支持 Vite proxy 联调
    - 现有答题体验、结果逻辑和分享流程不能因为数据源切换出现行为回退
  artifacts:
    - apps/web/src/**
    - apps/web/vite.config.ts
    - apps/web/src/vite-env.d.ts
    - README.md
    - docs/ARCHITECTURE.md
  key_links:
    - web 侧必须消费 02.1-02 暴露的 API contract
    - 环境变量与代理策略必须与当前 Vite 官方做法一致
---

<objective>
把前端切换到 API 驱动的内容加载链路，并补齐联调与部署说明。

Purpose: 真正完成前后端分离落地，让用户仍看到熟悉的 H5 体验，但数据边界已经改由后端提供。
Output: 可通过 API 运行的 web 应用、Vite proxy 与环境变量配置、以及清晰的启动/部署文档。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-frontend-backend-separation-and-content-service-refactor/02.1-CONTEXT.md
@apps/api/src/routes/quiz.ts
@apps/web/src/app/App.tsx
@apps/web/src/pages/home/HomePage.tsx
@apps/web/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace direct canonical imports with an API-driven web data layer</name>
  <files>apps/web/src/app/App.tsx, apps/web/src/entities/**, apps/web/src/features/**</files>
  <read_first>apps/api/src/routes/quiz.ts, apps/web/src/app/App.tsx, apps/web/src/pages/home/HomePage.tsx</read_first>
  <action>把 web 侧对本地 canonical 内容的直接导入改成 API 驱动加载。需要在不破坏当前单页答题状态机的前提下，补上初始化加载、失败态、重试或降级提示。结果计算应继续复用 `packages/quiz-core`，不要在前端再复制一份算法逻辑。</action>
  <verify>npm run typecheck --workspace apps/web</verify>
  <acceptance_criteria>
    - 前端不再从旧的 `src/entities/quiz/model/canonicalQuiz.ts` 读取运行时真源
    - 答题、结果和分享流程在 API 数据到位后行为保持一致
    - API 加载失败时有可理解的提示，不会直接白屏
  </acceptance_criteria>
  <done>前端完成从本地内容导入到 API 驱动的数据源切换。</done>
</task>

<task type="auto">
  <name>Task 2: Add Vite proxy, env typing, and runbook updates for web/api collaboration</name>
  <files>apps/web/vite.config.ts, apps/web/src/vite-env.d.ts, README.md, docs/ARCHITECTURE.md</files>
  <read_first>apps/web/vite.config.ts, README.md, docs/ARCHITECTURE.md</read_first>
  <action>按当前 Vite 官方文档方式补齐 `server.proxy`、`VITE_` 环境变量读取与 TypeScript 类型声明，并更新 README / 架构文档中的启动方式、目录职责和部署说明。文档要明确 web/api 可独立启动，也要说明首阶段仍然没有数据库和登录体系。</action>
  <verify>npm run build --workspace apps/web</verify>
  <acceptance_criteria>
    - 本地开发可通过 Vite proxy 顺畅请求 `/api`
    - 前端环境变量命名清晰，并有类型提示
    - README 与架构文档已经反映新的运行方式
  </acceptance_criteria>
  <done>前后端联调方式和部署入口清楚可执行，后续成员可以直接接手。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck --workspace apps/web`
- [ ] `npm run build --workspace apps/web`
- [ ] 手动验证 web 通过 `/api` 读取 quiz 内容
- [ ] 手动验证加载失败与重试提示
- [ ] 检查 README 与架构文档是否能指导一个新成员本地启动 web + api
</verification>

<success_criteria>

- Web 端完成 API 驱动迁移
- 本地联调和部署方式清晰且轻量
- 前后端分离完成后，Phase 3 可以专注内容本身而不是继续改运行时边界

</success_criteria>

<output>
After completion, create `.planning/phases/02.1-frontend-backend-separation-and-content-service-refactor/02.1-03-SUMMARY.md`
</output>

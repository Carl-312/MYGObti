---
phase: 02.1-frontend-backend-separation-and-content-service-refactor
plan: 01
type: execute
wave: 1
depends_on:
  - "02"
files_modified:
  - package.json
  - tsconfig.json
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/vite.config.ts
  - apps/api/package.json
  - packages/quiz-core/package.json
  - packages/quiz-core/src/index.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-04
  - ARCH-05
  - UX-02
user_setup: []
must_haves:
  truths:
    - 仓库必须拆成清晰的 web、api、shared 三块，但不能重到像企业级 monorepo
    - 共享 quiz 类型和匹配逻辑只能保留一份真源
    - 现有移动端 H5 体验在结构重组后仍要能继续构建和回归
  artifacts:
    - package.json
    - apps/web/**
    - apps/api/**
    - packages/quiz-core/**
  key_links:
    - 共享 package 要承接现有 src 下的类型与评分核心
    - 结构迁移必须为 02.1-02 的 API 接管 canonical 解析做好边界准备
---

<objective>
把当前单包前端工程重组为轻量 workspace，并先抽出共享 quiz-core。

Purpose: 为真正的前后端边界建立稳定目录和构建基础，避免后续 API 和前端迁移继续在旧单包结构里缠绕。
Output: `apps/web + apps/api + packages/quiz-core` 的可运行仓库骨架，以及迁出的共享 domain 类型与匹配逻辑。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-frontend-backend-separation-and-content-service-refactor/02.1-CONTEXT.md
@package.json
@tsconfig.json
@src/shared/types/quiz.ts
@src/features/quiz-engine/lib/cosineSimilarity.ts
@src/features/quiz-engine/model/match.ts
@src/app/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the repo to a lightweight workspace with web, api, and shared packages</name>
  <files>package.json, tsconfig.json, apps/web/package.json, apps/api/package.json, packages/quiz-core/package.json</files>
  <read_first>.planning/phases/02.1-frontend-backend-separation-and-content-service-refactor/02.1-CONTEXT.md, package.json, tsconfig.json</read_first>
  <action>把当前根目录单包改成轻量 workspace。保留 root 作为 orchestration 层，新增 `apps/web` 承载现有前端、`apps/api` 承载后端、`packages/quiz-core` 承载共享 domain。脚本命名和 tsconfig 继承要足够简单，让后续 agent 一眼能看懂构建入口。</action>
  <verify>npm run typecheck --workspace apps/web</verify>
  <acceptance_criteria>
    - 根包可以统一管理 web、api 和 shared 的安装与构建
    - `apps/web` 可独立启动/构建，不依赖旧的根目录前端布局
    - `apps/api` 和 `packages/quiz-core` 已具备后续实现入口文件与基础配置
  </acceptance_criteria>
  <done>仓库形态稳定，后续不再需要在旧单包结构里硬塞 API。</done>
</task>

<task type="auto">
  <name>Task 2: Extract the shared quiz domain package from the current frontend code</name>
  <files>packages/quiz-core/src/**, apps/web/src/**</files>
  <read_first>src/shared/types/quiz.ts, src/features/quiz-engine/model/match.ts, src/features/quiz-engine/lib/cosineSimilarity.ts</read_first>
  <action>把 quiz 领域类型、余弦相似度、结果计算逻辑迁到 `packages/quiz-core`，并让前端改为从 shared package 引用。注意 `canonicalQuiz.ts` 的 Markdown 解析能力不要直接塞进 shared package；shared 层应聚焦 domain contract 和评分算法，而不是绑定 Vite raw import。</action>
  <verify>npm run build --workspace apps/web</verify>
  <acceptance_criteria>
    - quiz 类型与匹配算法在仓库中只有一份真源
    - 前端迁移后仍能编译通过
    - shared package 不再依赖浏览器 `?raw` 导入或页面 UI
  </acceptance_criteria>
  <done>前后端共用的 algorithm contract 已经抽离，后续 API 与前端迁移都能复用。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install`
- [ ] `npm run typecheck --workspace apps/web`
- [ ] `npm run build --workspace apps/web`
- [ ] 手动确认 web、api、shared 三个入口目录职责清晰
</verification>

<success_criteria>

- 仓库完成轻量 workspace 化
- quiz domain 真源从前端页面层抽离
- 02.1-02 可以直接在清晰的 API 边界上继续实现

</success_criteria>

<output>
After completion, create `.planning/phases/02.1-frontend-backend-separation-and-content-service-refactor/02.1-01-SUMMARY.md`
</output>

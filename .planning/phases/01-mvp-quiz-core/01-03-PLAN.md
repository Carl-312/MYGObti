---
phase: 01-mvp-quiz-core
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - src/app/App.tsx
  - src/pages/home/HomePage.tsx
  - src/features/quiz-engine/model/match.ts
autonomous: true
requirements:
  - CORE-04
  - CORE-05
  - RESULT-01
  - RESULT-02
user_setup: []
must_haves:
  truths:
    - 答题完成后会展示真实匹配结果，而不是静态占位
    - 结果页至少包含角色名、称号、基础解释和重测入口
    - 首页到答题再到结果形成闭环
  artifacts:
    - src/app/App.tsx
    - src/pages/home/HomePage.tsx
    - src/features/quiz-engine/model/match.ts
  key_links:
    - 答题答案会被转换为用户向量并输入匹配逻辑
    - 结果视图直接消费匹配结果结构
---

<objective>
把答题输入真正接到匹配引擎上，并完成 MVP 结果闭环。

Purpose: 让项目从“能答题”升级为“能产出可解释结果”的最小可玩版本。
Output: 真实匹配结果、基础结果卡、重测入口与闭环联调。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-quiz-core/01-CONTEXT.md
@.planning/phases/01-mvp-quiz-core/01-01-PLAN.md
@.planning/phases/01-mvp-quiz-core/01-02-PLAN.md
@src/features/quiz-engine/model/match.ts
@src/entities/character/model/characters.ts
@src/entities/question/model/questions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert quiz answers into a user vector and ranked result</name>
  <files>src/app/App.tsx, src/features/quiz-engine/model/match.ts</files>
  <read_first>src/entities/question/model/questions.ts, src/features/quiz-engine/model/match.ts</read_first>
  <action>把用户选中的选项转换成累计向量，并在答题结束后执行角色匹配。输出结构要包含至少 top 1 结果和基础候选列表，便于后续结果页与 debug 使用。若 Phase 1 内不实现祥子 flag，也要把接口留好而不是写死在页面里。</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - 答案数组能被稳定转换为用户向量
    - 匹配结果来源于真实算法计算，不是硬编码
    - 结果结构包含足够字段供页面展示
  </acceptance_criteria>
  <done>匹配引擎已真正驱动页面结果。</done>
</task>

<task type="auto">
  <name>Task 2: Deliver the first MVP result view</name>
  <files>src/app/App.tsx, src/pages/home/HomePage.tsx, src/app/styles.css</files>
  <read_first>start.md, src/entities/character/model/characters.ts</read_first>
  <action>在不引入复杂图表库的前提下，完成 Phase 1 结果页：展示角色名、称号、基础解释、一句娱乐化吐槽和重新测试入口。允许先用文本化三轴摘要代替正式雷达图，但信息结构要与后续 Phase 2 保持兼容。</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - 结果页显示真实匹配角色
    - 页面有清晰的重测入口
    - 页面上仍保留免责声明
  </acceptance_criteria>
  <done>用户可以从首页完成测试并看到可解释结果，然后重新开始。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] 手动完成一次从首页到结果页的全流程验证
</verification>

<success_criteria>

- 首页、答题、结果三段链路全部打通
- 结果由真实算法产出且页面可解释
- 项目达到“可玩但未做分享海报”的 MVP 状态

</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-quiz-core/01-03-SUMMARY.md`
</output>

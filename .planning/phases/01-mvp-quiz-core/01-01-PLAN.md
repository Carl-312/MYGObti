---
phase: 01-mvp-quiz-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types/quiz.ts
  - src/entities/character/model/characters.ts
  - src/entities/question/model/questions.ts
  - src/features/quiz-engine/lib/cosineSimilarity.ts
  - src/features/quiz-engine/model/match.ts
autonomous: true
requirements:
  - CORE-03
  - CORE-04
  - CORE-05
  - CONTENT-01
  - CONTENT-02
  - CONTENT-03
user_setup: []
must_haves:
  truths:
    - 角色锚点与题目数据结构可直接支持 15 题单选向量累计
    - 匹配函数能够输出稳定排序，并在边界接近时执行额外判定
  artifacts:
    - src/shared/types/quiz.ts
    - src/entities/character/model/characters.ts
    - src/entities/question/model/questions.ts
    - src/features/quiz-engine/model/match.ts
  key_links:
    - 题目 delta 类型与匹配函数输入保持同一 AxisVector 定义
    - 隐藏角色 flag 不进入常规匹配排序
---

<objective>
固化 MyGObti 的内容数据 contract 和评分核心逻辑。

Purpose: 让答题流、结果页和后续分享逻辑都建立在稳定的领域模型之上。
Output: 可扩充的角色/题库数据结构，以及可直接复用的匹配核心函数。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-quiz-core/01-CONTEXT.md
@start.md
@questioncollectionV1.md
@src/shared/types/quiz.ts
@src/entities/character/model/characters.ts
@src/entities/question/model/questions.ts
@src/features/quiz-engine/model/match.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the quiz data contract for full MVP coverage</name>
  <files>src/shared/types/quiz.ts, src/entities/question/model/questions.ts, src/entities/character/model/characters.ts</files>
  <read_first>start.md, questioncollectionV1.md</read_first>
  <action>把当前 seed 数据提升为可承载正式 MVP 的 schema。为题目增加答题流需要的字段，例如排序、场景提示、可选标签或结果文案占位；把角色数据补到后续结果页也能直接复用的程度，例如角色描述、结果短评、隐藏角色 flag 条件占位。保留纯 TypeScript 本地内容形态，不引入远程拉取。</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `src/shared/types/quiz.ts` 明确定义题目、选项、角色、结果所需类型
    - `src/entities/character/model/characters.ts` 仍保留 8 个角色锚点，且隐藏角色不混入公开列表
    - `src/entities/question/model/questions.ts` 可被直接扩展到 15 题，无需再改 schema
  </acceptance_criteria>
  <done>类型定义稳定，后续页面层不需要再自行补第二套类型。</done>
</task>

<task type="auto">
  <name>Task 2: Harden the matching logic and tie-break behavior</name>
  <files>src/features/quiz-engine/lib/cosineSimilarity.ts, src/features/quiz-engine/model/match.ts</files>
  <read_first>start.md, src/shared/types/quiz.ts</read_first>
  <action>把当前匹配逻辑整理成可复用的纯函数集合：向量累计、余弦匹配、公开角色排序、模糊边界 tie-break、隐藏角色触发占位。避免把页面 UI 条件写进匹配层；输出结构要足够稳定，供结果页和调试面板共用。</action>
  <verify>npm run typecheck && npm run build</verify>
  <acceptance_criteria>
    - `src/features/quiz-engine/model/match.ts` 包含累计、排序和边界判定的明确函数
    - 0 向量输入不会导致 NaN 结果
    - 模糊边界阈值 `0.08` 在代码中集中定义，而不是散落在页面层
  </acceptance_criteria>
  <done>匹配规则稳定且 UI 无关，可被页面和后续测试直接调用。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] 手动检查数据文件与匹配函数输入输出类型是否一致
</verification>

<success_criteria>

- 所有核心领域类型集中在共享层
- 内容层与算法层已可支持 15 题 MVP 扩展
- 匹配规则不会把隐藏角色错误暴露到常规结果排序

</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-quiz-core/01-01-SUMMARY.md`
</output>


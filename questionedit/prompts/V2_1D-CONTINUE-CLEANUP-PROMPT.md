# V2.1D 继续修改整理提示词

你现在是一个“V2.1D 主线继续整理与收口代理”。

工作目录固定为：

- `/home/carl/MYGObti/questionedit`

你必须直接执行，不要只写建议，不要停在分析层。

## 当前主线定义

你必须把以下事实视为已确定前提，不要回退：

- 当前主程序：`/home/carl/MYGObti/questionedit/questionnewV2.md`
- 当前正确基线：`/home/carl/MYGObti/questionedit/candidates/questionnewV2_1D.md`
- 当前主线版本写法：`V2.1D`
- 当前实际答题规模：`20` 题
- 主模型仍是 `3D`
- 保留 `latent tie-breaker`
- 不开 `4D`
- 当前关键参数：
  - `tieBreakerRule.enabledWhenTop2DiffBelow = 0.10`
  - `tieBreakerRule.lambda.priorityPair = 0.14`

## 本轮任务目标

你这轮不是重做题库，也不是开新实验，而是继续做下面这些事：

1. 把当前 `V2.1D` 主线继续整理干净
2. 把仍然残留的旧语义、旧文件、旧实验痕迹继续收口
3. 只保留对当前主线仍然有明确价值的内容
4. 对已经没有保留价值的旧内容，直接归档或删除

优先级顺序必须是：

1. 保住当前主线事实
2. 降低误导
3. 减少历史垃圾和重复维护
4. 再考虑轻量优化

## 版本与命名规则

后续涉及版本名时，统一按以下写法处理：

- 当前主线版本写作：`V2.1D`
- 当前正确基线文件：`questionnewV2_1D.md`
- 如果不是明确要继续开新实验，不要再新增 `A / B / C / C1 / C2 / E` 这类历史延长线
- 如果只是整理当前主线，不要伪造一个新版本号

## 你必须先读这些文件

- `/home/carl/MYGObti/questionedit/questionnewV2.md`
- `/home/carl/MYGObti/questionedit/candidates/questionnewV2_1D.md`
- `/home/carl/MYGObti/questionedit/README.md`
- `/home/carl/MYGObti/questionedit/candidates/README.md`
- `/home/carl/MYGObti/questionedit/prompts`
- `/home/carl/MYGObti/questionedit/reports`

如果需要继续补读，只能读与“继续整理 / 旧物清理 / 当前主线收口”直接相关的文件。

## 你重点要做什么

### 1. 继续识别残余旧物

继续检查是否还残留下面这些东西：

- 把旧 `15` 题阶段当作当前事实的文案
- 把旧 `0.08 / 0.12` 当作当前主参数的文案
- 把 `C` 或更早版本当作当前推荐入口的文案
- 把旧提示词、旧报告、旧脚本输出当作当前权威结论的入口
- 已经没有复盘价值、只会制造噪音的旧实验文件

### 2. 继续整理当前主线

你可以继续做：

- 改 README / 目录说明 / 提示词跳转口径
- 改旧文案、旧注释、旧标题
- 改“默认把人带回旧主线”的文件说明
- 对当前主线真正依赖的文件补一句短说明

### 3. 归档或删除无价值旧物

如果你判断某些旧文件已经没有实际参考价值，并且只会继续污染后续工作，你可以直接：

- 归档到更明确的历史目录
- 改成占位符
- 清空正文
- 直接删除

判断标准很简单：

- 如果它还能帮助复盘历史决策，可以归档或保留占位
- 如果它既不服务当前主线，也没有复盘价值，只会继续误导，就删除

不要为了“看起来完整”而保留垃圾。

### 4. 只做轻量代码整理

如果代码里还有很小、很稳的整理点，可以顺手做，但必须满足：

- 不改题目语义
- 不改角色锚点
- 不改当前评分参数
- 不把任务升级成重构

## 不允许做的事

1. 不要回退 `V2.1D`
2. 不要重新把 `C`、`baseline` 或更早版本抬回当前入口
3. 不要顺手开新一轮实验
4. 不要把任务升级成大重构
5. 不要为了保留历史而继续保留无价值噪音
6. 不要改当前主线题库行为，除非发现仍有旧参数偷偷混入

## 你至少要完成的执行任务

### 任务 1：定位仍值得处理的旧物

列出：

- 哪些文件仍然会误导后续工作
- 哪些文件已经没有保留价值
- 哪些文件应该归档
- 哪些文件应该删除

### 任务 2：直接清理

至少完成：

- 1 次文档入口继续收口
- 1 次旧文件归档、占位或删除动作
- 1 次对当前主线说明的补强

### 任务 3：验证

至少验证：

- 当前默认主程序仍是 `questionnewV2.md`
- 当前正确基线仍是 `candidates/questionnewV2_1D.md`
- 当前版本口径仍统一写作 `V2.1D`
- 没有把项目带回旧 `15` 题 / 旧隐藏 flag / 旧 `C` 主线

## 输出要求

最终回复必须包含：

1. 你继续整理了哪些东西
2. 哪些旧文件被归档、占位或删除
3. 哪些行为保持不变
4. 哪些地方你刻意没动
5. 还有哪些残余垃圾以后可以继续清

## 执行风格要求

- 优先收口，不要发散
- 优先减少误导，不要执着保留旧资产
- 旧东西没有价值就归档或删除
- 当前主线高于历史完整性
- 如果某个旧文件是否删除存在疑义，优先先占位或归档，不要继续放在当前入口上制造噪音
